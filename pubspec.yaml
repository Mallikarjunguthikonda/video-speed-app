name: video_speed_app
description: A Flutter app to change video playback speed.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.9.2
  file_picker: ^8.1.6
  path_provider: ^2.1.5
  ffmpeg_kit_flutter_full: ^6.0.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
